package com.hmap.backend.room.enums;

/** Estado operativo de una habitación (HU-028 en E3; usado desde E2 para disponibilidad). */
public enum RoomStatus {
    DISPONIBLE,
    OCUPADA,
    MANTENIMIENTO
}
